# DISABLED: Articles and books data now live in portfolio-data repo (root articles.json, books.json).
# This site fetches from https://raw.githubusercontent.com/ozzgio/portfolio-data/main/articles.json
# and books.json at build time. To regenerate data, update portfolio-data (e.g. via n8n or gh).
name: Generate Articles and Books JSON (disabled)

on:
  workflow_dispatch:
    inputs:
      reason:
        description: 'Data is in portfolio-data repo; this workflow is no longer used.'
        required: false

jobs:
  skip:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Articles/books are in portfolio-data repo. No-op."
